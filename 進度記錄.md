# 迦南人事差勤系統 — 開發進度記錄
更新日期：2026年6月14日

---

## 系統架構

| 元件 | 說明 |
|------|------|
| `index.html` | 員工手機端（LINE LIFF App） |
| `admin.html` | 管理員後台 |
| `code.gs` | Google Apps Script 後端 |
| `env.js` | 環境設定（GAS URL、LIFF ID） |

**GAS Web App URL**（見 env.js）  
**LIFF ID**：`2010383109-EolilvJ0`  
**GitHub**：https://github.com/samhi123/canancard

---

## Google 試算表結構

### 員工檔案
| 欄 | 欄位 |
|----|------|
| A | 員工編號 |
| B | 姓名 |
| C | 部門 |
| D | 職稱 |
| E | 到職日 |
| F | 聯絡電話 |
| G | LINE UID（格式：U開頭+32碼） |
| H | 在職狀態 |
| I | 權限設定（Admin / 一般） |
| J | 員工密碼 |
| K | 所別 |
| L | 基本薪資（數字） |
| M | 員工生日（yyyy-MM-dd） |
| N | Email |

### 打卡紀錄
`員工編號 | 姓名 | 日期 | 時間 | 打卡類型 | 備註`

### 請假申請
`表單ID | 員工編號 | 姓名 | 表單類型 | 假別/項目 | 開始時間 | 結束時間 | 合計時數 | 事由 | 審核狀態 | 審核人 | 審核時間`

### 加班申請
`表單ID | 員工編號 | 姓名 | 加班類型 | 開始時間 | 結束時間 | 合計時數 | 事由 | 審核狀態 | 審核人 | 審核時間`

---

## code.gs — 已實作功能

### doPost（寫入）
| action | 功能 |
|--------|------|
| `clockInOut` | 員工打卡，寫入「打卡紀錄」 |
| `submitForm` | 送出請假/加班申請，依 formType 寫入對應工作表 |
| `deleteForm` | 員工撤回申請（僅限「待審核」狀態） |
| `bindLineUid` | 員工自助綁定（目前保留，未啟用） |
| `reviewForm` | 主管審核表單，更新審核狀態、審核人、審核時間 |
| `saveHRProfile` | 儲存/修改員工檔案 |
| `deleteHRProfile` | 刪除員工檔案 |

### doGet（讀取）
| action | 功能 |
|--------|------|
| `verifyLogin` | 用 LINE UID 查員工，回傳 empId、name、role |
| `getHRProfiles` | 取得員工清單 |
| `getPendingForms` | 取得待審核表單（從「請假申請」） |
| `getMyForms` | 取得指定員工的所有請假/加班申請 |
| `getAttendanceReports` | 取得打卡紀錄 |

---

## index.html — 員工端功能

- **打卡首頁**：上班/下班打卡，顯示今日狀態
- **表單申請**：請假（事假/病假/特休/婚喪假）、加班申請
- **申請進度追蹤**：顯示該員工的所有申請，可撤回「待審核」的申請

### 登入流程
1. LIFF 初始化 → 取得 `profile.userId`（LINE UID）和 `profile.displayName`
2. 呼叫 GAS `verifyLogin` 比對員工檔案
3. **找到** → 設定 `currentEmployee.empId` 和 `name`，顯示工號和權限
4. **找不到** → 畫面顯示該員工的 LINE UID，請員工截圖回報給管理員

### 重要細節
- `env.js` 需與 `index.html` 放在同一目錄，在 `<head>` 最先載入
- 打卡和表單送出使用 `mode: "no-cors"`（看不到回應，只能從試算表確認）
- 申請進度用 GET 請求，可正常讀取回應
- 日期顯示格式：民國年，例如「115年6月14日 上午9：00」

---

## 待處理 / 可延伸功能

- [ ] `admin.html` 的後台功能尚未完整確認（主管審核、員工管理）
- [ ] `getPendingForms` 目前只查「請假申請」，加班申請的待審核尚未整合
- [ ] 員工打卡的 `empId` 依賴 LINE UID 正確比對才能取得真實工號（需確認所有員工的 LINE UID 已正確填入員工檔案）
- [ ] 加班申請的「撤回」目前用 category="加班" 對應「加班申請」工作表，欄位索引與請假不同（審核狀態在第8欄，請假在第9欄）

---

## 每次修改後的必要步驟

1. 修改 `code.gs` → 貼回 Google Apps Script → **部署 → 管理部署 → 編輯 → 建立新版本 → 部署**
2. 修改 `index.html` / `env.js` → 推送到 GitHub（`samhi123/canancard`）
